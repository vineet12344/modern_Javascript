var buttonStyle = document.getElementById("button-increase")
var heading = document.getElementById("experiment")

var initialFont = window.getComputedStyle(heading).font

heading.style.fontSize = initialFont